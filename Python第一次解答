class Solution:
    def twoSum(self,nums,target):
        c = len(nums)
        for i in range(c):
            j = target - nums[i]
            if j in nums: 
                x = nums.index(j) 
                if i == x: 
                    continue 
                else: 
                    return i,x 
                    break
            else: 
                continue 


这是对于leetcode上边一道两数之和的解答。
